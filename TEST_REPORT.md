# v0.2测试记录

日期：2026-09-22。

## 已执行

在项目目录运行 `python3 -m unittest discover -s tests -v`：24项通过，包含原有10项检索测试和14项RAG/接口测试。接口测试使用模拟响应，没有向真实模型服务发送请求。

覆盖：关键词检索、来源映射、输入限制、空检索不调用模型、模拟模式标记、JSON格式和结构校验、伪造或未检索引用拦截、缺失引用、合理拒答、模型响应接入、HTTP错误隐藏敏感正文、输出截断、接口地址与配置校验，以及实验记录防覆盖。

特别保留一个边界测试：错误的语义主张附带合法引用ID时，ID校验仍会通过。这说明当前校验不等于事实核查，不能把引用合法率当作证据支持率。

## 开发集检索检查

运行 `python3 evaluate_retrieval.py`，结果保存于eval/retrieval_pilot.json。

10个开发题中6题有参考卡片，4题无参考卡片被跳过。每题指标为命中的参考卡片数/该题参考卡片数，最多检索4张卡；6题均值83.3%。其中D04“我在淡水边，应该先看哪里？”未命中。

这只是小型开发集的参考覆盖检查，不是问答准确率、测试集泛化结果或生态预测准确率。

## 运行示例

已执行mock食性问题并保存eval/mock_example.json。synthetic=true，model_called=false；它是流程演示，不可作为真实大模型实验结果。

## 尚未验证

- 真实模型账户、参数兼容性及实际付费调用。
- 真实生成答案的证据忠实性。
- 主动澄清、多轮状态和条件检查Agent。
- 独立测试集上的系统比较。
- 实地鱼情、中鱼率或建议的生态有效性。

软件测试只支持上述特定行为，不证明整个研究假设成立。
