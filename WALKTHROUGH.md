# 从零理解这份代码

## 1. 先看数据，不急着看代码

打开 data/knowledge_cards.json。每个大括号对象是一张卡片，id是编号，claim是资料摘要，applicability是条件，application_note是我们对它的使用方式，source_ids指向来源。

再打开 sources.json，根据S01之类的编号查来源。两个文件分开后，一份来源支持多张卡片时，不必重复维护链接。

## 2. 看 app.py：程序入口

`main()`先读取命令行参数。`--query`后面是问题；没有这个参数时，程序循环等待键盘输入。

`KnowledgeBase()`创建知识库对象，载入本地文件。`kb.search(query)`返回字典。`format_result()`把字典变成适合人阅读的文字。

从输入到输出，只要先理解这三步：读问题、查资料、显示资料。

## 3. 看 retriever.py：真正的检索发生在哪里

`DATA_DIR`根据代码文件的位置找到data目录，因此从其他目录启动也不会找错文件。

`CONCEPTS`是我们手工整理的同义词表。例如水草、植被、草区属于一个概念，用户不必输入与卡片完全相同的词。

`KnowledgeBase.__init__()`读取JSON，检查编号是否重复、引用的来源是否存在。错误资料应被发现，而不是悄悄传给未来的大模型。

`search()`先检查输入和物种范围，再比较问题与卡片词语。匹配越明确，内部排名越靠前；最多返回4张。这个排名只表示文本匹配程度，不代表有鱼概率。

没有匹配时返回empty，不编造内容。命中结果完整保留卡片的地域条件与来源限制。

## 4. 为什么现在没有AI回答

目前完成的是可检查的数据检索。下一阶段大模型接收这些结果并组织语言，形成“检索增强生成”。随后加入追问和工具选择，才逐渐成为我们规划的Agent。

拆开开发可以知道错误来自哪里：检索错了、资料错了，还是模型解释错了。

## 5. 你可以做的第一个练习

运行“水草”和“草丛边有软饵可以试吗”，观察两个问题的返回。然后看CONCEPTS中vegetation和softworm的词表，理解为什么不同说法能检索到相关卡片。

再输入“火星钓鱼”，确认无结果。不要为了让所有问题都有回答，给不相关的卡片也设置非零分数。
